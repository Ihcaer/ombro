import { Route } from '@angular/router';
import { LoginComponent } from './pages/login/login.component';

const authRoutes: Route[] = [{ path: 'login', component: LoginComponent }];

export default authRoutes;
