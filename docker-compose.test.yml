services:
  db:
    ports:
      - "5432:5432"

  minio:
    ports:
      - "9000:9000"
      - "9001:9001"
      
  mailpit:
    image: axllent/mailpit:v1.28
    ports:
      - "1025:1025"
      - "8025:8025"
    
  backend:
    env_file:
      - ".env.test"

  frontend_admin:

  nginx:
    ports:
      - "5000:80"
    environment:
      NGINX_PORT: 80
      SSL_ENABLED: "false"
      HSTS_MAX_AGE: 60
      RATE_LIMIT_ENABLED: "false"